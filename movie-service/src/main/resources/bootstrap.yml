spring:
  application:
    name: movie-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ${config-server:localhost}
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://${eureka-service:localhost}:8761/eureka
