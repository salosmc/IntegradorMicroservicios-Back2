spring:
  application:
    name: movie-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: localhost

  profiles:
    active: dev

queue:
  movie:
    name: Movie

eureka:
  client:
    service-url:
      defaultZone: http://${eureka-service:localhost}:8761/eureka
