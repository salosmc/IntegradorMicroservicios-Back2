spring:
  zipkin:
    enabled: true
    baseUrl: http://${zipkin:localhost}:9411
  rabbitmq:
    username: guest
    password: guest
    host: ${rabbitmq:localhost}
    port: 5672

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true
  endpoint:
    info:
      enabled: true
